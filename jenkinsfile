pipeline {
    agent none
    stages {
        stage('Build docker') {
            agent { lable Agent }
            steps {
                echo 'Hello Docker Agent'
                sh 'docker images'
            }
        }
    }
}