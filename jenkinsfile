pipeline {
    agent none
    stages {
        stage('Build docker') {
            agent { label 'Agent' }
            steps {
                echo 'Hello Docker Agent'
                sh 'docker images'
            }
        }
    }
}
