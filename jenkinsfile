pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            label 'JenkinsBuild'
        }
    }
    stages {
        stage ('List images') {
            steps {
                sh 'docker images'
            }
        }
    }
}

